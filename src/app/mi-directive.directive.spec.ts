import { MiDirectiveDirective } from './mi-directive.directive';

describe('MiDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MiDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
